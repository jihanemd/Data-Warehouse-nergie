# ====================================
# CONFIGURATION DASHBOARD & API
# ====================================

# Base de données PostgreSQL
DB_USER=postgres
DB_PASSWORD=jihane
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dw_energie_france

# Schema
DB_SCHEMA=gold

# API Flask
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=True
FLASK_ENV=development

# Dashboard
DASHBOARD_HOST=localhost
DASHBOARD_PORT=8000

# Cors
CORS_ENABLED=True
CORS_ORIGINS=*

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/api.log

# Cache
CACHE_ENABLED=True
CACHE_TTL=300

# Security
SECRET_KEY=your-secret-key-here-change-in-production
JWT_SECRET=your-jwt-secret-here

# Notifications
ALERTS_ENABLED=True
EMAIL_ALERTS=False
SLACK_WEBHOOK=

# Features
REAL_TIME_UPDATE=True
UPDATE_INTERVAL=5

# Version
APP_VERSION=1.0.0
APP_NAME=Energy DW France Dashboard

# ====================================
# NOTES DE CONFIGURATION
# ====================================
#
# 🔐 Sécurité:
#    - Modifier DB_PASSWORD en production
#    - Générer des SECRET_KEY aléatoires
#    - Ne pas commiter ce fichier en git
#
# 🌐 Réseau:
#    - DB_HOST: localhost (machine locale) ou IP serveur
#    - FLASK_HOST: 0.0.0.0 (accessible réseau) ou localhost
#    - CORS_ORIGINS: * (développement) ou domaines spécifiques (prod)
#
# 📊 Performance:
#    - CACHE_TTL: Durée cache requêtes (en secondes)
#    - UPDATE_INTERVAL: Fréquence actualisation temps réel
#
# 📝 Logs:
#    - Créer dossier logs/ avant de lancer l'app
#    - Consulter logs/api.log pour erreurs
#
